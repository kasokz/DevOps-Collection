# Useful Setups for Container environments

Here are some Setups to be used with Container runtimes.
The container runtim of choice is Docker.

For that following is needed:
1. Docker installed
2. docker-compose CLI tool installed